/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};
// Dummy comment to trigger Vercel clean redeployment and resolve platform EnvFileReadError
module.exports = nextConfig;

